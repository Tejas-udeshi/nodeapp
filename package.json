{
  "name": "node-app",
  "description": "hello test app",
  "version": "0.0.1",
  "private": false,
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^8.0.1",
    "supertest": "^4.0.2"
  }
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "node index.js"
 }
}
